steps:
  - task: AzureCLI@2
    displayName: Get inference cluster
    inputs:
      azureSubscription: $(ml_workspace_connection)
      scriptType: bash
      scriptLocation: inlineScript
      inlineScript: |
        python templates/src/python-sdk/get_inference_cluster.py -f config-aml.yml
